import { writable } from 'svelte/store';



export const webResults = writable({});
export const imageResults = writable({});
export const newsResults = writable({});
export const videoResults = writable({});